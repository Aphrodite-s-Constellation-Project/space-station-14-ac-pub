name: "Labels: Approved"
on:
  pull_request_review:
    types: [submitted]

jobs:
  add_label:
    # Change the repository name after you've made sure the team name is correct for your fork!
    if: ${{ (github.repository == 'Aphrodite-s-Constellation-Project/space-station-14-ac-pub') && (github.event.review.state == 'APPROVED') }}
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
    - name: "👥 Проверить права ревьювера"
      uses: tspascoal/get-user-teams-membership@v3
      id: checkUserMember
      with:
        username: ${{ github.actor }}
        organization: "Aphrodite-s-Constellation-Project"
        team: "team-maintainers,junior-maintainers"
        GITHUB_TOKEN: ${{ secrets.PROJECT_TOKEN }}
    
    - name: "🏷️ Добавить ярлык 'Одобрено'"
      if: ${{ steps.checkUserMember.outputs.isTeamMember == 'true' }}
      uses: actions-ecosystem/action-add-labels@v1
      with:
        labels: ":white_check_mark: Одобрено"
